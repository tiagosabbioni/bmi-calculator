import React from "react";
import { View } from "react-native";
import Form from "../form";

const Main = () => {
    return (
        <View style={{flex: 1}}>
            <Form />
        </View>
    );
}

export default Main;